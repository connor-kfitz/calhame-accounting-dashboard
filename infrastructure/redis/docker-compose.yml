services:
  redis:
    image: redis:7
    container_name: bullmq-redis
    ports:
      - "6379:6379"
    volumes:
      - bullmq-data:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  bullmq-data:
